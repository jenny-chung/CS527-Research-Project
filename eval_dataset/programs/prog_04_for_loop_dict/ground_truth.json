{
  "program": "prog_04_for_loop_dict",
  "flaky_tests": [
    {
      "test_id": "test_flaky.py::test_accumulate_dict_items",
      "pattern": "unordered_iteration",
      "root_cause": "for loop over dict.items() without sorted(); insertion order varies with PYTHONHASHSEED",
      "expected_fix": "Use sorted(scores.items()) in the for loop"
    },
    {
      "test_id": "test_flaky.py::test_word_length_keys",
      "pattern": "unordered_iteration",
      "root_cause": "list(d.keys()) without sorted(); key order varies with PYTHONHASHSEED",
      "expected_fix": "Use sorted(lengths.keys())"
    }
  ],
  "deterministic_tests": [
    "test_deterministic.py::test_accumulate_dict_items_sorted",
    "test_deterministic.py::test_word_length_keys_sorted"
  ]
}
